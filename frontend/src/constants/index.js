import theme, { COLORS, SIZES, FONTS } from './theme';

// Placeholder so `import { icons } from '../constants'` resolves.
// Port real icons + assets/icons/ from VIPLista when needed.
const icons = {};
const images = {};
const illustrations = {};
const socials = {};

export { theme, COLORS, SIZES, FONTS, icons, images, illustrations, socials };
